import React from "react";

const NotFound = () => (
  <div className="not-found">
    <h3>Page Not Found</h3>
    <p>Sorry! We can't seem to find that page. Please try again.</p>
  </div>
);

export default NotFound;
